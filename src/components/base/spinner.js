import React from 'react';

const Spinner = () => {
    return (
        <div className="spinner-wrapper">
            <span className="spinner"/>
        </div>
    );
};

export default Spinner;